import { ContentManagementServiceApi } from 'apps/website-display/redux/features/ManageContentServiceApiSlice';

export const AnswersApi = ContentManagementServiceApi.injectEndpoints({
  endpoints: builder => ({


  })
});

export const {

} = AnswersApi;
