import { Box } from "@mui/material";
import icon from "./connectionProblem.png";
import React from "react";

const ConnectionProblemIcon = () => {
	return (
		<Box
			component="img"
			src={icon}
		/>
	);
}

export default ConnectionProblemIcon;